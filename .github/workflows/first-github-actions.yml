name: Run Shell Commands
on: [push,fork]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: echo "Hello Security Gurus!! - This is our first Github Actions Workflow YML file"